# Council seat — raw agent output

**task-id**: `a5589e783e994c885`  
**brief opens**: You are a council member with authority over a supervisor agent. Its owner has removed its self-assessment authority. It did not write your brief.  Your seat's lens: **th…

---

## The estate is dead right now, and reporting success

I did not have to reconstruct the failure from logs. It is happening as I write.

```
2026-08-19T19:19:56Z   (now)
$ tmux ls
Hill90: 1 windows          ← my own session. That is all.
```

`agent-supervisor` has not existed since 17:15. `director` has not existed since 17:16. Meanwhile:

```
$ cat .poller-recovery-last-success
2026-08-19T19:18:12Z        ← 104 seconds ago
$ cat .poller-recovery-fail-streak
0
```

**The estate recorded a successful recovery, ninety seconds ago, for a session that has been gone for two hours.** That is not a metaphor. That is the contents of the file the digest reads.

---

## 1. Detection, action, or ownership

**Not detection. Not even notification. Action — and specifically, ownership of action.**

Detection is not merely present, it is excellent. `watchdog.status` right now:

```
state:    no_session
detail:   tmux session 'agent-supervisor' does not exist
poller:   dead — zero live inbox-poll.sh processes by pid; recovery handles this
never-busy: unknown — session 'agent-supervisor' does not exist (failed 31 check(s) in a row)
```

The diagnosis is exact, correctly worded, and correctly severe. My subagent's inventory counted **24 detectors against 14 actuators — of which 3 fire, 5 have no trigger at all, and 6 are gated behind `tmux has-session`**, the precondition that is false in precisely the outage they exist for. I verified the load-bearing ones myself.

And notification works. It is not a blocked channel:

```
19:06:02Z NEVER-BUSY-CHECK-ESCALATE: sent via telegram
19:15:09Z NEVER-BUSY-CHECK-ESCALATE: sent via telegram
```

88 Telegram messages delivered today. Jon is being paged every nine minutes that the estate is dead. **He is being told, and it is still dead.** Any prescription of the form "detect it better" or "alert him sooner" is answered by the log file before it is proposed.

The estate contains **43 code sites whose terminal action is a string**. The literal phrase `a human should look` appears in `heartbeat.sh`, `quota-watch.sh`, `director-loop.sh`, and `watchdog.sh`. `director-loop.log` has written it 13 times today, on a 15-minute timer, into a file with no reader.

---

## 2. The recovery path, traced to its end

`poller-recover.sh:155`:

```bash
tmux has-session -t "$SESSION" 2>/dev/null || { log "no session '$SESSION' -- nothing to recover into"; exit 0; }
```

**`exit 0`.** The script's first substantive line converts total estate death into a success exit code. It then never reaches the 145 lines of exhaustively-reasoned edge-case handling below it — the mkdir lock, the orphan check, the window-id ambiguity refusal, the `lsof` PATH fix. Eight edge cases reasoned through in 120 lines of header comment. The one that actually happens has a one-line early return and **zero test coverage** (`test_poller_recover.sh` contains no assertion for it).

`watchdog.sh:1073-1082` consumes that exit code:

```bash
rc=$?
if [ "$rc" -ne 0 ]; then
  ... streak=$((streak + 1)) ...
else
  printf '0' >"$POLLER_RECOVERY_FAIL_STREAK"
  printf '%s' "$iso" >"$POLLER_RECOVERY_LAST_SUCCESS"
fi
```

So the answer to "does the estate report success while dead" is **yes, and worse than passively**. It does not merely fail to notice. It *affirmatively writes a fresh success timestamp and zeroes the failure counter* on every tick of a total outage. The two state files created specifically so a failure could not go unrecorded across ticks (`#41`, after recovery failed 37 consecutive times unnoticed) are the two files now certifying health during a two-hour death.

The watchdog's own top-level branch does the same thing:

```bash
if ! tmux has-session -t "$(lanes_session_or_default)"; then
  report no_session "tmux session '...' does not exist"
  log "no ... session"; exit 0        # ← no notify, no repair
fi
```

`report()`'s comment says it plainly: *"escalate is the only state a human needs told about."* `no_session` — the most severe state the system can be in — is not `escalate`. It pages nobody. The pages Jon actually receives are a **side effect of a parse failure** in a lane-scanning check, wearing the title `NEVER-BUSY-CHECK FAILED`. He is being told his check is broken, not that his estate is down.

And `watchdog.sh:955` hardcodes the reassurance:

```bash
log "POLLER-CHECK: zero live inbox-poll.sh processes by pid — dead-poller recovery handles this"
```

`recovery handles this` is a string literal, not a measurement. It is false in exactly the case that produces it.

**106 ticks of `no session` across four days** — 2 on 08-14, 32 on 08-15, 41 on 08-18, 31 today. Each outage ended only when a human typed something. `restart-history` is zero bytes.

---

## 3. Who can create a session from nothing

Two lines in the estate can:

- `bootstrap-session.sh:260` — `run new-session -d -s "$SESSION" ...`
- `restore.sh:201` — `"$TMUX_BIN" new-session -d -s "$target_session" ...`

I grepped for every invocation of either, excluding comments and tests, across the repo, the live copy, all nine `~/Library/LaunchAgents/` plists, and `crontab -l`.

**`crontab -l` is empty. No plist references either script. No script invokes either script.**

The complete set of callers is:

1. `README.md:37` — a command for a human to type.
2. `supervisor_view.py:671` — an MCP tool, in a server that is not registered in any MCP client config.

**Nothing schedules session creation. The estate's only path from "dead" to "alive" is Jon reading his own README.** Said plainly, as asked: there is no scheduled, unattended path that creates a tmux session that does not currently exist, and there never has been.

Corroborating: five launchd jobs are loaded. All five are detectors. `com.jonhill.director-loop` has `last exit code = 3` and has been failing every pass for two hours.

---

## 4. Is anything reading the detectors' output

Split verdict, and the split is the finding.

**State files are read and do gate behavior.** `.poller-recovery-fail-streak`, the episode JSONs, `.quota-watch.state` all have real consumers that branch and escalate. This part works.

**The logs are write-only.** `watchdog.log` — 18,900+ lines — has **zero readers in the estate.** `poller-recover.log`, `director-loop.log`, `heartbeat.log`: no reader. Every line quoted in this report I read by hand; nothing in the system does.

That matters because of what routes around it. `digest.sh` reads `watchdog.status`, never `watchdog.log`. So the recovery *mechanism* failing was invisible to the digest — which is exactly the defect `#41` was filed for and exactly what is happening again, one layer up: the log says `no session ... nothing to recover into` 106 times, and the status field says `last confirmed recovery: 19:18:12Z`.

And when a page does fire, it names the wrong subsystem. `watchdog_notify.py:299` hardcodes `"inbox-poll heartbeat stale ... cat ~/.local/state/.../inbox-poll.status"` for all three subscribers. Every heartbeat page ever sent (`threshold 600s` × 3) was the **quota-watch** check telling Jon to inspect the inbox-poll file. The real inbox-poll heartbeat check (`threshold 210s`) has never paged once.

---

## 5. Closed-not-done, and guards that cannot fire

The sharpest specimen is one I tested myself. Open issue **#347** carries this acceptance block, captioned *"Fails while restore.sh has no branch for a non-tmux lane id"*:

```bash
$ grep -qE "claude.print|harness_session_id|detached" scripts/supervisor/restore.sh; echo rc=$?
rc=0
```

**It already passes.** `harness_session_id` appears in `restore.sh` for unrelated reasons. The issue's own stated failing test is green while the bug is entirely unfixed. That is the acceptance mechanism itself being decorative.

The rest, verified by subagent with citations:

- **#366** — an estate-wide launchd exit sweep, closed by comment with no PR, deferred to a mechanism that does not read `launchctl`. It would be paging right now: `director-loop` is sitting at exit 3. Its second ask — "a scheduled job must not depend on which branch a working tree is on" — was applied to one job. **Four of five launchd jobs still execute from the shared git working tree** at `79bb081`, while `live/` is at `0e2e08e`; `director-loop.sh` differs by 40 lines between the two.
- **#325** — closed COMPLETED, no PR, no comment, **neither ask performed.** Its "must NOT nudge a quiet-but-healthy pane" test is structurally unreachable: the only suite touching it pins `HEARTBEAT_STALE_AFTER=0` in all three cases, forcing the stall branch every time.
- **#382** — shipped `poller-leak-cleanup.sh` (183 lines, 9 tests) with **zero callers.** Run once by hand. This is verbatim the anti-pattern the repo names in its own test harness: *"the `acp_transport.py` shape: a tested mechanism with no caller."*
- **`heartbeat.sh:197`** — the stall nudge verifies success by grepping the whole pane for `esc to interrupt`, which is a substring of the message it just typed. It always reports success; line 200 is unreachable. The file's own header documents this exact trap and fixes it correctly at line 93, then reintroduces it 104 lines later.
- **`ci_gate.py:93`** — both gate workflows declare their job as `gate:`, so both emit a check run of the same name. De-duplication keys on name alone; the later one erases the earlier. Demonstrated against PR #394's real SHA: ui-evidence's **failure** was discarded because fixpass succeeded three seconds later. The merge gate is decided by a race.
- **`watchdog_notify.py:336`** — the `state: stopped` staleness exemption is unbounded in time and is live: `inbox-poll.status` has read `stopped` since 17:15, so the alarm is permanently suppressed, by design.

---

# WOULD IT HAVE WORKED

Against the four recorded outages (08-14, 08-15, 08-18, 08-19).

| Candidate | Verdict |
|---|---|
| Another detector for session death | **No.** Already detected, verbatim, on tick one of all four. |
| Alert Jon faster / better channel | **No.** Telegram delivered 88 messages today. He was told every 9 minutes for 2 hours. |
| Fix `poller-recover.sh` to return non-zero on missing session | **Partial.** Stops the false success stamp. Repairs nothing. Outage still ends only by hand. |
| A watchdog "supervisor health" dashboard | **No.** `watchdog.status` already contains a correct diagnosis nobody acts on. |
| Wire `restore.sh --session agent-supervisor` into the `no_session` branch | **No — actively harmful.** Verified by dry-run: `--session` is a *redirect*, not a filter (`restore.sh:121` overwrites `target_session` for every row). It plans **156 restores into a 10-window session**, targeting `agent-supervisor:2` five times, pulling in lanes from `skills`, `agent-tui` and old `agent-dotfiles`. |
| Wire bare `restore.sh` (no flag) | **No.** Dry-run: resurrects **five** sessions including abandoned ones and the test session `ad241repro-22535`, burning quota on 34 lane resumes nobody wants. |
| Auto-restart lanes on death | **No, and rightly rejected.** AGENTS.md invariant 3: *"a fresh agent wearing a recovered lane's name looks fully healthy and has none of the context."* That reasoning is correct and must survive any fix. |
| **`bootstrap-session.sh` on `no_session`, then scoped `restore.sh`** | **Yes.** Dry-run clean, exit 0, would create the session + 9 honest `free-N` lanes and register it via `adopt-session`. `restore.sh` then adopts those shells with real conversations and leaves the unrecoverable ones as honest free lanes. Needs the scoping bug fixed first. |

---

# WRONG PRESCRIPTIONS

**1. "Add monitoring."** The prescription that gets built after an outage like this, and the one that would do the most damage — it consumes the remediation budget while leaving the fault untouched. This estate has 24 detectors, and every one of them is right. Building a 25th produces a system that is even more articulate about its own death.

**2. "Make the alerting louder."** Escalation already fires and delivers. Louder pages train Jon to filter them, which is worse than the current state.

**3. "Just call the recovery script on `no_session`."** The obvious one-line fix. I dry-ran it. Both invocations are wrong — one collapses the entire ledger into a single session, the other resurrects four abandoned ones. **The remedy is broken because it has never been run**, and it has never been run because nothing calls it. Prescribing it without executing the dry-run first is how a council makes things worse.

**4. "Auto-restart the lanes."** Explicitly and correctly rejected by invariant 3. Do not reopen it. Note the boundary the estate itself already drew: invariant 8 says *"the poller is a service, not a lane."* The refusal to invent lane continuity is right; it was silently over-extended to the **session container**, which has no continuity to fake.

**5. "The fix is that `poller-recover.sh` should return 1."** Treats the symptom — the false success — and leaves the estate dead, now with an accurate report of being dead. Necessary, insufficient, and dangerous if mistaken for a fix.

---

# THE UNASKED QUESTION

**Every actuator in this estate is gated behind a refusal-on-uncertainty, and total death is the state of maximum uncertainty. The safety posture is perfectly anti-correlated with the failure mode.**

Read them together:

```
poller-recover: "no session ... nothing to recover into"          → exit 0
watchdog:       "tmux session does not exist"                     → exit 0
heartbeat:      "target session does not exist; a human should look"
director-loop:  "target session does not exist -- a human should look"
quota-watch:    "refusing to guess which is the supervisor pane"
restore:        "refuse, never invent"
heartbeat:      "quota UNKNOWN (rc=2) -- refusing to nudge on an unreadable meter"
```

Each of those refusals is individually well-reasoned, and several were hard-won from real incidents that destroyed work. Collectively they compose into a system that becomes **monotonically less able to act as its situation worsens** — and that has never been designed, reviewed, or questioned as a whole. Nobody chose it. It accreted, one correct local decision at a time.

The corroborating evidence: **80 open issues, 12 about sessions, and not one says "nothing can create a session."** #389 (deploy deadlock), #359 (claims never released), #346 (hardcoded window ids), #347 (restore can't place lanes) — every one is a *refinement of a refusal*. The root is not among them because it is not visible from inside any single script. Each file is locally correct.

And the mechanism built to distinguish "sessions this system owns" from "Jon's own" — the `sessions` ledger table, `#153` — currently reads:

```
at14-scratch-safe    at14-scratch-nogit    at14-scratch-busy    agent-tui    skills
```

Three test scratch sessions and two side projects. **Neither production session is in it.** The registry that should answer "what am I responsible for keeping alive" has never contained the two things the entire estate exists to keep alive. That is the ownership gap in one row-set: nothing is registered as owned, so nothing owns bringing it back.

---

# WHAT TO ACTUALLY DO

Ranked. Each names a mechanism, not a resolution. None depends on an agent noticing it is failing.

### 1. Register the production sessions as owned, and make ownership the trigger

`INSERT` `agent-supervisor` and `director` into the `sessions` table; remove the three `at14-scratch-*` rows. Then `watchdog.sh`'s `no_session` branch calls `bootstrap-session.sh` **for any session in that table that does not exist** — and for nothing else.

This is the whole fix, and it is why it goes first: it converts "recovery" from a judgement call into a set-difference between a table and `tmux ls`. No guessing, no scope creep, no risk of resurrecting `ad241repro-22535`.

> **Enforcement:** a shell test under `tests/supervisor/` that starts an isolated tmux server (`TMUX_TMPDIR`, per invariant 4), registers a session in a throwaway ledger, kills it, runs one watchdog tick, and asserts the session exists afterward. `test_shell_suites.py:23` globs `test_*.sh` and CI runs it via `unittest discover` — I verified this wrapper exists and is real, so a shell test here is genuinely enforced, not decorative. Mutation-check it: revert the watchdog change and confirm the test goes red.

### 2. Make `poller-recover.sh:155` exit non-zero, and stop the watchdog stamping success it did not earn

`exit 0` → `exit 1` on missing session. Independently, `watchdog.sh`'s success branch must not write `.poller-recovery-last-success` on a tick where `report`'s state was `no_session` — the trap runs the checks on every exit path, which is how a dead estate certifies itself healthy.

Do this second, not first: alone it is a symptom fix. Paired with #1 it becomes the failure signal that proves #1 ran.

> **Enforcement:** the case has **zero test coverage today** — `test_poller_recover.sh` has no assertion for a missing session, despite 120 lines of header reasoning about eight rarer ones. Add it to that suite. Assert both the exit code and that `.poller-recovery-last-success` is *unchanged* across the tick.

### 3. Fix `restore.sh`'s scoping before anything automates it

Split the flag: `--session` (redirect, current behavior, human-only) from `--only-session` (filter, the one a scheduler may use). Close #347 first — `restore.sh` cannot place claude-print lanes, which is the transport the estate now actually runs on.

Until this lands, **do not wire `restore.sh` to anything.** I proved both current invocations are destructive.

> **Enforcement:** `test_restore.sh` gains a case asserting `--only-session X` emits **no** plan line whose lane prefix is not `X`. And replace #347's acceptance grep — it passes today against unfixed code; make it a real placement test.

### 4. A launchd exit sweep — #366's unperformed second ask

One check reading `launchctl list | grep com.jonhill.` and escalating on a repeated non-zero. It would page **right now** (`director-loop`, exit 3, failing for two hours), and it would have paged on the day #366 was filed and closed.

Also from #366, unperformed: move the four jobs still executing from `/Users/jon/source/repos/Personal/agent-supervisor/scripts/` onto `live/`. Their working tree is at `79bb081` while `live/` is at `0e2e08e`, and `director-loop.sh` differs by 40 lines between them. Only `supervisor-watchdog` runs from `live/`.

> **Enforcement:** a test asserting every `com.jonhill.*` plist's `ProgramArguments[0]` resolves under `$SUPERVISOR_LIVE`. It fails today, four times. Fix it, and branch drift can never again decide what guards the estate.

### 5. Repair the three guards that cannot fail

- **`ci_gate.py`** — rename one of the two `gate:` jobs. One line. A red UI-evidence check currently vanishes on a three-second race; the merge gate is a coin flip.
- **`heartbeat.sh:197`** — apply the `| tail -1 |` discipline already correct at line 93 in the same file. The nudge currently verifies itself by matching the text it just typed.
- **`watchdog_notify.py:299`** — parameterize the hardcoded `inbox-poll` message. Every page ever sent named the wrong subsystem and the wrong file.

> **Enforcement:** for the first, a workflow-lint test asserting no two workflows declare the same job name — this class recurs and a name collision is machine-checkable. For the second, the empirical reproduction already exists (idle footer as last line, submitted prompt in scrollback); pin it. For the third, assert the paged threshold matches the calling check's threshold — the mismatch (`600s` in a message titled `inbox-poll`, whose real threshold is `210s`) is the assertion.

### 6. Write down the composition rule that nobody owns

One paragraph in AGENTS.md, as an invariant: **a refusal-to-act must name what does act instead, or it is a bug.** Every one of the 43 human-terminal sites either gets an actuator or gets an explicit `# NO ACTUATOR BY DESIGN:` with a reason.

This is the only item that is documentation, and it is last because documentation is not enforcement — but it is the thing that would have prevented the composition from forming.

> **Enforcement:** a grep test that fails on any *new* `a human should look` string without an adjacent `NO ACTUATOR BY DESIGN` marker. Grandfather the 43 existing sites explicitly, by count, so the number can only go down. A count that can only decrease is a mechanism; an intention to be careful is not.

---

**One correction to my own brief, since the seat demands it:** the prompt's framing invited me to find that detection exists and does nothing. That is true but understates it. Detection exists, notification exists, *delivery* exists, and the human is being paged. The estate is not silent — it is articulate, correct, and continuously ignored by itself. The thing that has never existed, in any version, on any schedule, is a line of code that creates a session. That is the whole of it.